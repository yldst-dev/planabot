## 변경 사항

- 텔레그램 전송 전 응답을 1024토큰 기준으로 한 번 더 정리해 장문 답변과 문장 중간 `출처:` 삽입 문제를 줄였습니다.
- Vertex Express thinking 설정을 env에서 제어할 수 있도록 확장했고, `gemini-3-flash-preview`에서 `off`를 사실상 `MINIMAL`로 적용하도록 맞췄습니다.
- planabrain 응답 타임아웃을 30초에서 180초로 늘려 긴 검색 응답이 중간에 실패 처리되는 문제를 완화했습니다.
- 프라나 기본 시스템 프롬프트를 현재 대화 스타일 기준으로 보강했습니다.
- README와 예시 env를 최신 운영 설정 기준으로 갱신했습니다.
